[
  {
    "service_name": "Github",
    "name": "Push on repository",
    "description": "trigger when a push is made on the given repository",
    "parameters": [
      {
        "name": "repository",
        "type": "string",
        "description": "the full name of the repository (e.g., user/repo)"
      }
    ],
    "trigger_types": ["webhook"]
  },

  {
    "service_name": "Area",
    "name": "Every x minutes",
    "description": "trigger every x minutes",
    "parameters": [
      {
        "name": "every_minutes",
        "type": "number",
        "description": "the interval in minutes (default is 5)"
      }
    ],
    "trigger_types": ["Polling"]
  }
]
